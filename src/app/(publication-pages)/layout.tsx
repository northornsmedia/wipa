import PlatformLayout from "@/app/platform/layout";

export default function PublicationPagesLayout({ children }: { children: React.ReactNode }) {
  return <PlatformLayout>{children}</PlatformLayout>;
}
